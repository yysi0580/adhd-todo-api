services:
  - type: web
    name: adhd-todo-api
    env: docker
    plan: free
    healthCheckPath: /api/v1/health
    envVars:
      - key: ENVIRONMENT
        value: production
      - key: DATABASE_URL
        sync: false
      - key: AUTO_CREATE_TABLES
        value: false
